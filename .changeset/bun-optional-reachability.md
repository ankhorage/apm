---
'@ankhorage/apm': patch
---

Preserve Bun optional dependency path semantics so frozen-install verification requires packages reached by mandatory paths while allowing optional-only subtrees to remain absent.
